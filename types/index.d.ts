export interface TaskState {
  taskname: string;
  finished: boolean;
  current: boolean;
}
export interface UserState {
  fullname: string;
}
export interface InfoState {
  name: string;
  on: boolean;
}
